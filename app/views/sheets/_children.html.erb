<%#= f.fields_for :students do |builder| %>
  <%#= render 'student_fields', f: builder %>
<%# end %>
  <ul>
    <% @details.each do |detail| %>
      <li>
        <%= link_to detail.child.name, child_detail_sheet_path(@sheet, detail: detail.id), remote: true %>
      </li>
    <% end if @details.present? %>
  </ul>