<%= form.select :period, [['Monthly','monthly'],['Yearly','yearly']], :selected => @object.plan.present? ? @object.plan.period : 'yearly', disabled: !@object.persisted? || @object.admin_id.present? ? false : true %>

